<virtualhost *:80>
        ServerName wernicke.zone

        ProxyRequests On
        ProxyPass / http://localhost:3000/
</virtualhost>

<virtualhost *:80>
        ServerName base.wernicke.zone

        ProxyRequests On
        ProxyPass / http://localhost:27017
</virtualhost>

<virtualhost *:80>
        ServerName raffin-j-dev.wernicke.zone

        ProxyRequests On
        ProxyPass / http://localhost:2105/
</virtualhost>
